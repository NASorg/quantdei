here <- rprojroot::find_rstudio_root_file()
library(glue)
library(tictoc)
library(ggplot2)
source(glue("{here}/code/setup.R"))

rds_files <- list.files(glue("{here}/data/twitter/twitter_account_dei_term"), full.names = T)

parse_feed <- function(rds){
  
  
  
  
  
  
}
